const express = require("express"); const app = express(); app.get("/health",(req,res)=>res.json({status:"ok"})); app.get("/status",(req,res)=>res.json({uptime:process.uptime(),memory:process.memoryUsage()})); app.get("/ready",(req,res)=>res.json({ready:true})); app.get("/metrics",(req,res)=>res.json({requests:0,errors:0})); app.listen(10000,"0.0.0.0",()=>console.log("Diagnostics server wired and running on 10000"));
